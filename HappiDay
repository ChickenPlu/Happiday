<!DOCTYPE html>
<html lang="vi">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Chúc mừng sinh nhật</title>
<style>
    body {
        margin: 0;
        background: #000;
        overflow: hidden;
        font-family: Arial, sans-serif;
    }

    /* TRÁI TIM RỰC RỠ */
    .heart {
        position: absolute;
        top: 50%;
        left: 50%;
        width: 180px;
        height: 160px;
        transform: translate(-50%, -50%);
        background: red;
        filter: drop-shadow(0 0 25px red) drop-shadow(0 0 40px #ff4d6d);
        animation: pulse 1.3s infinite ease-in-out;
        clip-path: path('M90 30 C90 0 0 0 0 60 C0 120 90 160 90 160 C90 160 180 120 180 60 C180 0 90 0 90 30 Z');
    }

    @keyframes pulse {
        0%,100% { transform: translate(-50%, -50%) scale(1); }
        50% { transform: translate(-50%, -50%) scale(1.2); }
    }

    /* DÒNG CHỮ CHẠY NGANG */
    .text-line {
        position: absolute;
        width: 100%;
        white-space: nowrap;
        font-size: 28px;
        color: #ffebf0;
        font-weight: bold;
        text-shadow: 0 0 10px #ff4d6d, 0 0 20px #ff99aa;
        animation: marquee 8s linear infinite;
    }

    @keyframes marquee {
        0% { transform: translateX(100%); }
        100% { transform: translateX(-100%); }
    }
</style>
</head>
<body>

<div class="heart"></div>

<script>
    const messages = [
        "Chúc mừng sinh nhật!", 
        "Chúc bạn tuổi mới hạnh phúc!", 
        "Luôn vui vẻ và thành công!", 
        "Mong mọi điều tốt đẹp đến với bạn!", 
        "Sinh nhật thật rực rỡ nha!"
    ];

    messages.forEach((text, i) => {
        const line = document.createElement("div");
        line.className = "text-line";
        line.style.top = `${10 + i * 15}%`;
        line.style.animationDelay = `${i * 1.2}s`;
        line.innerText = text;
        document.body.appendChild(line);
    });
</script>

    <!-- 5 dòng chữ chạy thêm -->
    <div class="run-text" style="top:20%">💖 Chúc em luôn xinh đẹp và hạnh phúc 💕</div>
    <div class="run-text" style="top:30%; animation-delay:1s">✨ Tuổi mới thật nhiều niềm vui ✨</div>
    <div class="run-text" style="top:40%; animation-delay:2s">🌸 Mong mọi điều tốt đẹp đến với em 🌸</div>
    <div class="run-text" style="top:60%; animation-delay:3s">💫 Luôn cười thật tươi nha bé 💫</div>
    <div class="run-text" style="top:70%; animation-delay:4s">🎀 Gà thương BTrâm nhiều lắm 🎀</div>

</body>
</html>
